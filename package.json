{
  "name": "superproxy",
  "version": "1.0.0",
  "description": "",
  "main": "./build/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && babel --presets es2015,stage-2 -d dist/ src/ && cp src/*.json dist/"
  },
  "bin": {
    "superproxy": "./dist/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "argv": "0.0.2",
    "express": "^4.14.0",
    "proxy-lists": "git://github.com/markbaas/proxy-lists.git"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-2": "^6.18.0"
  }
}
